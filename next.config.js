/** @type {import('next').NextConfig} */
const nextConfig = {
  swcMinify: true,
  /*i18n: {
    locales: ["en"],
    defaultLocale: "en",
    domains: [
      {
        domain: "itinder.com",
        defaultLocale: "en-US",
        http: true,
      },
    ],
  },*/
};

module.exports = nextConfig;
